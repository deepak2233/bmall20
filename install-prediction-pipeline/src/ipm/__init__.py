"""Install Prediction Model (IPM) — end-to-end training & serving pipeline.

The package is intentionally small and explicit so that every step (loading,
feature engineering, model fitting, ONNX packaging, serving) can be read and
explained in isolation.
"""

__version__ = "0.1.0"
